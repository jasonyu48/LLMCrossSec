# Based on the packages actually used by this repo in the current `nlp` environment.
# If you use GPU inference/training, install the PyTorch build that matches your CUDA setup.

numpy==1.26.4
pandas==2.0.3
pyarrow==23.0.1
matplotlib==3.8.4
tqdm==4.67.3

torch==2.6.0
transformers==5.3.0
accelerate==1.13.0
bitsandbytes==0.49.2

yfinance==0.2.66
